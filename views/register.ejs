<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="icon" type="image/x-icon" href="/image/shubham_logo.jpeg">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registration Form</title>
    <link rel="stylesheet" href="/css/register.css" type="text/css">
</head>
<body>
    <div class="main">
        <div class="register">
            <h2>Register</h2>
            <form id="register" novalidate action="/mainlogin/<%= data[0].id %>/registerdone" method="post" >

                <label>First Name : </label>
                <br>
                <input type="text" name="fname"
                id="name" placeholder="Enter Your First Name" class="name"   required>
                <br><br>
                <label>Last Name</label>
                <br>
                <input type="text" name="lname"
                placeholder="Enter Your Last Name" class="name" required>
                <br><br>           
                <label>Your Age : </label>
                <br>
                <input type="number" name="age"  placeholder="How old are you?" class="name" required>
                <br><br>
                <label>Email : </label>
                <br>
                <input type="email" name="email"  value="<%= data[0].email %>" class="name" required readonly>
                <br><br>
                <label>Password</label>
                <br>
                <input type="password" name="password" value="<%= data[0].password %>" class="name" required readonly>
                <br><br>
                <label>Date Of Birth : </label>
                <br>
                <input type="date" name="dob" id="return-input" placeholder="" class="name" required>
                <br><br>
                <label>Gender : </label>
                <br>
                &nbsp;&nbsp;&nbsp; 
                <input type="radio" name="gender" id="male" >
                &nbsp;
                <span id="male">Male</span>
                &nbsp;&nbsp;&nbsp;&nbsp;
                <input type="radio" name="gender"  id="female">
                &nbsp;
                <span id="female">Female</span>
                <br><br>
                <label>Phone No. : </label>
                <br>
                <input type="tel" name="phoneno"  pattern="[0-9]{3}-[0-9]{3}-[0-9]{4}" placeholder="xxx-xxx-xxxx" class="name" required>
                <br><br>
               <button>submit</button>
                <!-- <button type="Edit">Edit</button> -->
            </form>
        </div>
    </div>

    <!-- <script>
            // let btn = document.querySelector("#submit");
            let forms = document.querySelectorAll("#register")
            
            // btn.addEventListener("click",()=>{
            //     if()
            // })
            // Example starter JavaScript for disabling form submissions if there are invalid fields
(function () {
  'use strict'

  // Fetch all the forms we want to apply custom Bootstrap validation styles to
//   var forms = document.querySelectorAll('.needs-validation')

  // Loop over them and prevent submission
  Array.prototype.slice.call(forms)
    .forEach(function (form) {
      form.addEventListener('submit', function (event) {
        if (!form.checkValidity()) {
          event.preventDefault()
          event.stopPropagation()
        }

        form.classList.add(':valid')
      }, false)
    })
})() -->
</body>
</html>
